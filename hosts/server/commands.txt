# Construire la VM

nix build .#nixosConfigurations.vps.config.system.build.vm

# Démarrer la VM

./result/bin/run-vps-vm

# Supprimer l'image de la VM
Supprime toutes les données ! Système vierge.

rm -f vps.qcow2